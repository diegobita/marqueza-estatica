{
  "name": "marqueza",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "dev": "tsnd --respawn --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "node app.js"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "ts-node-dev": "^2.0.0",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "env-var": "^7.4.1"
    
  }
}
